/*import { Curso } from '../Interfaces/curso';

export class CursosListas{
    seccion:number;
    turno:number;
    sucursal:number;
    gestion:number;
    constructor(){
        this.seccion=0;
        this.turno=0;
        this.sucursal=0;
        this.gestion=0;
    }
    muestraCurso(cursos:Curso[]){
        var aux:Array<Curso>=new Array<Curso>();
        cursos.forEach(iter=>{
         if(iter.seccion===this.secciones[this.seccion].id && 
            iter.turno===this.turnos[this.turno].id &&
            iter.gestion===this.gestiones[this.gestion]){
              aux.push(iter)
            }
       })
       return aux;
      }
}*/